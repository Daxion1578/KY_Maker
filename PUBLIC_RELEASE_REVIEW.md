# 公開前確認記録

確認日：2026-09-14  
対象版：v1.36.1-public  
状態：識別情報の除去・架空データ化済み／社外公開の承認と実機検証は未完了

## 結果

[公開用HTML](ai_ky_demo_public.html)と[公開用開発台帳](AI_KY_app_development_ledger_public.md)を作成しました。元ファイルは公開パッケージに含めていません。

この処理は「完全匿名化」「安全な公開」「社外でAIが動作すること」を保証するものではありません。添付された2ファイルだけを対象に、識別情報と実事例を除去した公開準備版です。

## 実施した処理

* 圧縮された実事例データを全て除去し、一般的な作業危険を題材にした新規の架空60件へ差し替えました。30種類のシナリオそれぞれに開始前・再開時の2つの模擬条件を設けています。実事例の匿名化・要約・復元可能な仮名化ではありません。
* 発生日は空欄、所属・場所は架空の汎用表記です。実在する個人名・企業名・設備名は新規データに使用していません。
* 帳票の会社表示、設備名を含む開発履歴、部署・内部サイトID、個人識別情報を含む共有URLを削除または一般化しました。本文だけでなくHTMLコメント・JavaScript文字列・圧縮データも対象にしています。
* 元DBの件数・分布・検索実例などの内部統計を公開コピーから除去しました。写真圧縮等の技術的な旧版測定記録は残しているため、その公開許諾は別途確認が必要です。
* 画面・AIプロンプト・印刷・JSON/CSVに公開デモ／架空事例の注記を加えました。既存JSONの一部の項目名は互換性のため残していますが、実際の発生記録を意味しません。
* CSV参照用の内部リンクを除去しました。追加の架空CSVは手動取込可能ですが、自動マスキングはありません。取込ボタンで注意を表示し、取込ファイル名は表示用の汎用名に置き換えます。

## 維持したもの

5画面構成、写真処理、作業チェック、3軸30タグ、TF-IDF・コサイン類似度、危険要因加点、タグ別候補抽出、AI選定と候補IDの照合、JSON/CSV出力、帳票を維持する方針で編集しました。検索の主要7関数はコメントを除くソース比較で変更がないことを確認しています。

ただし、データが変わったため検索順位・候補件数・AIの生成結果は元版と同じにはなりません。帳票の用紙サイズは固定せず、印刷時にブラウザ側でA4・横を選ぶ方式にしています。

## 検証結果

以下はファイル静的検査とNode.js上の関数テストです。実ブラウザの操作確認ではありません。

* JavaScript：2つのインラインスクリプトの構文検証に合格。
* 内蔵データ：実際の展開関数で60件を復元。6列形式、全件の日付空欄、索引生成を確認。
* 検索：全30タグに母集団があり、各タグに対応する架空の状況文を与えた検索で候補が返ることを確認。これは検索接続の確認であり、独立した安全性・精度評価ではありません。
* 注意点：「生物」タグだけの入力は該当なし。具体的な状況文を伴う場合は候補が返ります。既存の類似度しきい値は維持しています。
* プロンプト：候補IDが内蔵データに存在すること、候補方式と従来方式の双方で架空注記が入ることを確認。実AIによる選定や注記への従属は未検証。
* CSV：小さな架空CSVのパーサー試験に合格。画面からの取込・出力の通し試験は未実施。
* HTML：実属性としてのID重複なし、入力済みのテキスト欄なし、外部リソース参照属性なし。用紙サイズの最終指定がautoであることを確認。
* 再点検：元の圧縮データが残っていないことを確認。元の所属・所名（4文字以上）、元件名（10文字以上）、元本文（20文字以上）の完全一致転載は検出されませんでした。部分一致を網羅する匿名化保証ではありません。
* 識別情報：確認済みの会社・部門・設備・個人識別語、メール、共有URL、内部サイトIDについて残存を検出しませんでした。
* 認証情報：代表的なAPIキー・アクセストークン・秘密鍵の形式、および認証情報の文字列代入を点検し、該当を検出しませんでした。全種類の秘密情報の不存在を保証するものではありません。
* 画像：実写真の埋込みを検出しませんでした。既存のチェックマーク用SVGは残っています。将来取り込む写真の写り込みや位置情報は対象外です。

## 未実装・未確認

* AI接続：既存のホスト連携 `GleanBridge.callAgent` を維持しています。単体HTML／静的GitホスティングだけではAI解析は動きません。個人トークンを使う外部API接続は未実装です。
* 実機：ブラウザでの起動、5画面遷移、カメラ、実AI応答、帳票印刷、JSON/CSV出力は未確認です。
* 権利：ソースコード、帳票構成、技術記録、生成データの利用条件や公開許諾は確認していません。名称を削除しても公開可能になるとは限りません。
* リポジトリ：Git履歴、著者名・メール、ブランチ、Issue、README、設定ファイル、ログ、画像・その他の添付は確認対象外です。Gitへの投稿も行っていません。
* 追加入力：写真・CSV・自由入力・AI出力に後から入る個人情報・機密情報を自動除去する仕組みは実装していません。

## 公開・外部AI化に向けた推奨事項

以下は一般的な安全対策の提案であり、社内規程や承認済み設計ではありません。

* 公開前に権利者と所属組織の承認を得てください。実事例を含む旧ファイルや旧コミットを公開リポジトリに持ち込まないでください。
* 新規の公開リポジトリに、このパッケージの公開用ファイルだけを入れる方法を推奨します。コミット著者の実名・メールも公開範囲を確認してください。
* APIキーをHTML、JavaScript、Git、ブラウザストレージへ埋め込まないでください。サーバー側の秘密情報として保持し、認証付きの中継処理でAIへ接続する設計を推奨します。
* 公開デモには認証、利用回数制限、同時実行制限、入力サイズ制限、費用上限を設けてください。所有者のトークンを誰でも無制限に消費できる構成は避けてください。
* 公開前のGit履歴スキャンと、実ブラウザ・テスト用アカウントでの通し試験を実施してください。

## 公開コピーのSHA-256

配布後の差分確認用です。元ファイルのハッシュや匿名化前後の対照表は含めません。

* `ai_ky_demo_public.html`：`e7fdd92256c6d8aa9cc20ac2612836b521e58ea07ae4222206cb3442bc8d856d`
* `AI_KY_app_development_ledger_public.md`：`06c46a8b545b59322be7d1d71b54d7812aedf2119c0b146bd59cbd827f3ee3b8`

※重要な判断には必ず原文を確認してください。
