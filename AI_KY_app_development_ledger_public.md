# 現場写真AI-KY支援アプリ 開発実績・機能台帳（公開用）

最終更新 2026-09-14 / 公開用コピー v1.36.1-public（元HTMLの実装版はv1.36.0）

このファイルは開発の実績台帳です。機能追加や不具合修正を行う前に必ずこの台帳を確認し、過去に検証・却下した方式を再提案しないこと、既に実装済みの機能を壊さないことを確認します。作業後はこの台帳と、アプリ内の変更履歴（画面右上の版数バッジ）の両方を更新します。

## 1. アプリの位置づけ

現場写真をスマホで撮影し、AIが写真を目視して読み取った所見と、利用者が申告した作業条件を掛け合わせてKY（危険予知）案を作成する試作アプリです。画面・検索・帳票はHTML1ファイルに格納しています。ただしAI解析はホスト環境のブリッジに依存し、単体HTMLでは動作しません。

写真解析とKY案作成を検証する試作アプリです。公開版は特定の企業・部門・施設による提供、推奨、承認を示しません。AI判定は参考情報であり、責任者の確認を前提とします。

## 2. 現行の機能一覧（v1.36.1-public）

### 画面構成（5段）

| STEP | 画面 | 主な機能 |
|---|---|---|
| 1 | 写真登録 | カメラ直接起動、写真ライブラリ選択、ドラッグ＆ドロップ、解析モード選択 |
| 2 | 作業チェック | 判定タグ30種（軸A作業行為15／軸B場所・設備8／軸C環境・条件7）の複数選択、補足入力。解析は裏で進行し進捗をバナー表示 |
| 3 | 目視結果 | 待機画面（6工程の進捗可視化）、読み取り所見の表示・編集・承認 |
| 4 | KY案 | 危険度、危険箇所、KY4ラウンド、対策案、現地確認事項、一言行動目標 |
| 5 | 記録 | 確認状況・是正管理、記録サマリー、JSON／CSV出力、KY記録票の印刷 |

### 写真の取得と圧縮

* カメラ直接起動（`capture="environment"` で背面カメラ）。タッチ端末は写真枠タップでカメラ、PCはファイル選択
* 読み込み時に長辺1280pxへ正規化。HEIC/HEIFは変換不可のため検出して撮り直しを案内
* iOSはcanvasからWebPを書き出せないため、書き出し可否を接頭辞で検出しJPEGへ自動フォールバック
* 送信画質はモードごとの**最適動作点**を直接指定（速度優先 460px／標準 **520px**／精度優先 600px、いずれも**JPEG q22・4:2:0**）。通常は圧縮の試行1回で確定する（v1.27.2）
* 上限文字数を超えた場合のみ後退する。**画質はq22に保ち解像度のみを落とす**（520→460→400→340→300px）。等コスト比較で、同じ文字数なら解像度を優先する方が細部保持で有利なため。最終段のみq18へ

### 解析モード（利用者が選択、初期値は標準）

| モード | 長辺／品質 | 実測文字数 | 上限（安全網） | 分割数 | 想定所要 | 判読の目安 |
|---|---|---|---|---|---|---|
| 速度優先 | 460px／q22 | 約18.4千字 | 26,000字 | 4 | 約1分50秒〜2分20秒 | 開口部・柵・重機の位置・資材の積み方 |
| **標準** | **520px／q22** | 約23.0千字 | 32,000字 | 4 | 約2分20秒〜3分 | 積み段数・コーンの本数・鉄筋の配列 |
| 精度優先 | 600px／q22 | 約29.1千字 | 40,000字 | 5 | 約3分〜3分50秒 | 網目・小物金具・銘板の細部 |

所要時間は送信文字数にほぼ比例します（実測 **約5.6秒／千文字**、固定費はほぼゼロ）。上限文字数は動作点がまず通る値に置き、安全網としてのみ働かせます。

### AI呼び出し（2段構成）

第1段は画像のみを送り、目視所見だけを返させます。作業情報は渡しません。第2段は承認された所見と作業情報からKY案を生成し、画像を扱いません。

base64はチャンク分割してヒアドキュメントで追記させ、総文字数の照合とデコード後の画像検証を必須にしています。

### 進捗の可視化（6工程）

画面に表示する目安は**スマートフォン実機の実測値**です（v1.27.3）。サンドボックス内部処理の実測は実機の1/6以下のため使いません。

| 工程 | 内容 | 実機の目安（標準モード） |
|---|---|---|
| 1 | 写真を圧縮・送信準備 | 1秒未満 |
| 2 | AIへ接続 | 応答待ち（約4秒） |
| 3 | 画像データを転記 | **約2分10秒**（送信文字数から自動計算） |
| 4 | 画像を復元・検証 | 約2秒 |
| 5 | 画像を目視 | 約12秒 |
| 6 | 所見をまとめる | 約7秒 |

転記の目安は固定値ではなく、選択中のモードの実際の送信文字数×5.6秒／千文字で毎回算出します。モードを切り替えると目安も追従します（速度優先 約1分40秒／標準 約2分10秒／精度優先 約2分40秒）。

工程の切り替わりはAIの受信テキストから判定します。転記工程のみ「3 / 4 件」と件数を併記します。

### 処理時間の実測（v1.27.0）

STEP 3に「処理時間の実測」パネルを持ちます。区間を端末内（写真の読込・正規化、圧縮の試行、送信データの組立）とAI側（接続〜初回応答、画像データの転記、復元・検証、目視、所見整理）に分けて記録し、工程別の秒数・割合・横棒グラフで表示します。最長の工程を橙で強調し、その工程に応じた見立てを出します。転記はチャンク1件ごとの到着間隔も記録します。実行履歴の比較、報告用テキストのコピー・保存に対応します。

AI側の内部工程はブリッジが進捗を通知しないため、受信テキストの到達時刻から推定した値です。端末側の区間は直接計測した実測値です。

### 計測値を読むときの注意（v1.27.1で判明）

進捗マーカーの受信が0件の場合、AI側の工程は内訳に分けられません。この場合は「画像の転記〜目視（合算）」と表示され、個別の工程名は出しません。過去にこの区間が「画像の目視」と表示されていましたが、実際は転記を含む合計でした。

打ち切りには「総時間」と「沈黙（無通信60秒）」の2種類があります。最大沈黙が55秒を超えている場合は沈黙上限であり、総時間の短縮では解決しません。分割幅を狭めて出力の間隔を詰める必要があります。

圧縮の実時間はSTEP 1のモード選択時に消費されます。解析時の内訳に出る値はその実測値です。

### KY案の帳票様式（v1.28.0）

STEP 4で「一覧／ボード／記録様式」を切り替えられます。**データの持ち方は変えず、見せ方のみを切り替える**設計です。編集は従来どおり一覧側で行い、帳票は表示専用とします。

| 様式 | 元にした実物 | 構成 |
|---|---|---|
| ボード | 危険予知活動ボード（ホワイトボード） | 日付・工事件名・時間、危険のポイント／私たちはこうする（対策）の左右分割、4大重大災害・危険度の欄、結果AM・PM、記入欄8行、重点行動目標、供給支障・設備損壊の要因／対策、危険度と4大重大災害の凡例 |
| 記録様式 | 危険予知活動記録（用紙） | 左端の縦書きラベル帯、1ラウンド（重大性×可能性＝評価点）、2ラウンド（物的対策／基本動作による対策・マニュアル№・二人以上の確認・対策後の評価）、本日の行動目標、現場代理人、リスク評価の凡例 |

**印刷はA4横**で、表示中の様式のみを1枚に出します（罫線・塗りを保持、ブラウザの印刷設定でA4・横を選択。公開版は `@page{size:auto!important}` が優先）。画面上の倍率は印刷に持ち込みません。

#### 横長帳票の操作（v1.29.1）

スクロールバーからしか動かせず直感的でなかったため、**操作のみ**を改善しました。帳票の見え方はv1.28.0のままで、レイアウトには手を入れません。

* **掴んで動かす**: Pointer Events による指1本・マウス左ドラッグでの移動。スクロールバーに触れる必要をなくす
* **欄への寄せ**: 表の欄を軽く叩くとその欄が見える位置へ寄る（掴み移動の直後は誤作動を避けるため無視）
* **ホイール**: PCでは縦ホイールを横移動に読み替える
* **掴める条件**: 横に溢れているときのみ`grab`を付与し、画面の回転・幅変更に追従

CSSの追加は`cursor:grab`／`grabbing`と`overscroll-behavior-x`のみで、寸法や配色には影響しません。

##### v1.29.0で試して撤回した方式

ズーム操作バー（全体を表示／等倍／段階拡大）と`transform:scale`による拡大、スクロール枠への高さ制限・枠線・背景を入れましたが、**帳票のレイアウトが崩れたため撤去**しました。全体表示はスマホ幅で文字が5px程度になり判読できないという問題もありました。拡大が必要な場合は**ブラウザ標準の2本指操作**に委ねる方針とします。

#### 実物の欄を埋めるための変換

AIの出力にない項目は次の規則で補います。いずれも参考値であり、画面上に注記を出しています。

* **4大重大災害**: 想定災害・箇所・根拠の文言から正規表現で判定（感電／墜落・転落／挟まれ・巻き込まれ／重機接触）。該当なしは空欄
* **危険度**: AIの高・中・低を実物表記の大・中・小へ写す
* **重大性・可能性・評価点**: AIは出力しないため、危険度から重大性（大3・中2・小1）を割り当て、**4大重大災害に該当する場合は重大性を3へ引き上げる**。可能性は既定2。評価点は積で求め、6以上を赤・3〜4を黄・2以下を緑で表示
* **対策後の評価**: 重大性−1（最低1）

記録様式の採点は現場での見直しを前提とした叩き台です。実物の運用では重大性・可能性を現地で判断するため、**この値をそのまま使わないこと**を注記で明示しています。

### 類似ヒヤリハット検索（公開版）

内蔵していた実事例は全て削除し、新規の架空シナリオ60件に差し替えました。元の本文の匿名化・要約ではありません。発生日は空欄、所属と場所は架空表記です。元DBの母集団・内訳・検証実例・共有先は公開版には掲載しません。

* gzip＋base64、6列（発生日／危険要因／件名／状況／所属／所名）、US・RS区切りの形式を維持。
* 文字bi-gram、TF-IDF・コサイン類似度、危険要因加点、3軸30タグを維持。
* アプリがタグごとに最大100件の候補を抽出し、AIがタグごとに最大3件を選定して理由を返す構成を維持。架空データは60件のため、100件の候補が揃うことはありません。
* 事例IDは既存の `HH-00001` 形式を互換性のため維持。公開デモ内だけの番号で、旧版の番号との対応関係はありません。
* 本文・画面・AIプロンプトで架空事例であることを明示し、「当社で実際に発生した事象」という扱いを撤去。
* CSV取込機能を維持。公開許諾済みの架空CSVのみ使用し、既定の内部リンクは設けません。CSV取込ボタンで注意喚起しますが、内容の自動マスキング・機密判定機能はありません。
* 読込と索引作成は従来どおり端末内で実施します。入力後の事例はAIに送信され、記録にも含まれ得ます。
* 元DBの検索順位・被覆率・性能と同等であることは保証しません。検証済み範囲は末尾の公開版確認記録を参照してください。

### 過去事例をKY案に効かせる仕組み（公開版）

危険箇所の参考事例ID、選定理由、候補内順位、事例本文の照合・表示を維持します。存在しないIDは警告する構造を維持し、架空事例を実績の証拠として扱わない指示へ修正しました。

JSONの既存キー「実際に起きたこと」「過去事例からの示唆」は互換性のため残していますが、公開版では内容は架空シナリオです。JSONにデータ区分を追加し、CSVの対策欄にも公開デモ・架空事例の注記を加えています。追加のCSVを使う場合も架空データのみとしてください。

### 出力

記録JSON、SharePoint取込用CSV（`AnalysisMode`・`FacilityCondition`・`EnvCondition`・`AppVersion` 列を含む）、KY記録票の印刷（全パネル自動展開、版数フッター付き）。

## 3. 版数ごとの実績

| 版 | 区分 | 内容 |
|---|---|---|
| v1.36.1-public | 公開準備 | 実事例の全除去、架空60件への差し替え、識別情報と内部統計の除去。AI接続方式は未変更 |
| v1.0.0 | 構成 | 3画面遷移型、スマホ対応、カメラ直接起動、写真所見チェック廃止 |
| v1.1.0 | 表示 | 解析結果を折りたたみ式に再構成、AI出力に文字数上限 |
| v1.2.0 | 計測 | 圧縮比を6段階で実測し長辺640px・品質60を採用 |
| v1.3.0 | 機能 | 写真読み取り所見パネルを追加し解析の妥当性を検証可能に |
| v1.4.0 | 制御 | 60秒区切りで中間状態を引き継ぐ再開方式（最大6回） |
| v1.5.0 | 整理 | ダウンスケーリングを初回1回のみに統一 |
| v1.6.0 | 管理 | バージョン表示を追加（画面・記録・印刷票） |
| v1.7.0 | 修正 | AI呼び出しを2段に分割。往復上限不足による着手失敗と空判定の誤りを修正 |
| v1.8.0 | 構成 | 4画面遷移型へ変更。目視結果を専用画面で承認 |
| v1.9.0 | 撤去 | ポーリング（自動再送・再開）を撤去 |
| v1.10.0 | 修正 | 121秒での失敗に対し期限110秒・560px/q55・往復6へ |
| v1.11.0 | 検証 | 検証モード。アプリ側の上限をすべて撤去し原因を切り分け |
| v1.12.0 | 設計 | 第1段に作業情報を渡さない方式へ。初期値をプレースホルダ化 |
| v1.13.0 | 計測 | 沈黙の計測機構を追加（初回受信・最終受信・沈黙時間） |
| v1.14.0 | 修正 | 沈黙60秒の打ち切りを確定。base64のチャンク分割方式へ |
| v1.15.0 | 修正 | printf/echoで `+` `/` が欠落する問題をヒアドキュメントで解消 |
| v1.16.0 | 修正 | 読み取りが弱い原因（400px残存）を特定し640px・品質60へ |
| v1.17.0 | 性能 | WebP化で文字数38.7%削減、分割16→5に削減 |
| v1.18.0 | 修正 | iOSのWebP書き出し不可を検出しJPEGへフォールバック |
| v1.19.0 | 計測 | iPhone確定。30通り比較で600px・品質45を採用。HEIC対策 |
| v1.20.0 | 構成 | 5画面構成へ。作業チェック3群20項目。掛け合わせ方式を実装 |
| v1.20.1 | 表示 | チェックマークの色を枠色に統一（未定義CSS変数の修正） |
| v1.21.0 | 共有 | 作成時の初期表示データをすべて削除 |
| v1.22.0 | 体験 | チェック完了後は解析中でも次へ進め、STEP3を待機画面に |
| v1.23.0 | 修正 | 進捗が91%で止まる問題。実進捗（チャンクk/N）ベースへ |
| v1.24.0 | 表示 | 進行度を工程中心の表示に変更（6工程・目安秒数併記） |
| v1.25.0 | 修正 | iPhone実写真で300秒かかる問題。文字数を実測して自動調整 |
| v1.26.0 | 機能 | 解析モードの3段選択を追加 |
| v1.26.1 | 修正 | 起動処理の実行順序が逆でスクリプトが停止していた |
| v1.26.2 | 修正 | 画面遷移時にボタン列が再描画されず、v1.22.0の動線が失われていた |
| v1.27.0 | 計測 | 処理時間の実測機構。工程別の内訳表示、ボトルネックの強調と見立て、履歴比較、報告用の出力 |
| v1.29.0 | 操作性 | 横長帳票にズーム操作・全体表示・掴み移動を追加（※レイアウト崩れによりv1.29.1で一部撤回） |
| v1.33.0 | 改善 | 事例をKY案に効かせる。危険箇所への反映を義務化、参考事例欄と示唆欄を新設、ID照合で創作を検出 |
| v1.32.0 | 公開向け要約 | 事例データをgzip＋base64で内蔵。ブラウザで展開し、索引とタグ別プールを生成する。旧版の実事例・統計は公開版に含めない。 |
| v1.31.1 | 公開向け要約 | CSV参照先の設定を分離。公開版では内部共有リンクを削除し、公開許諾済みの架空CSVのみ手動取込する。 |
| v1.31.0 | 公開向け要約 | CSV取込と、文字bi-gramによるTF-IDF・コサイン類似度および危険要因加点を実装。対策の記録がない事例から対策を引用しない。 |
| v1.30.0 | 公開向け要約 | 類似ヒヤリハット検索とCSV差し替えを実装。公開版には旧版の実例ベースのサンプルを含めない。 |
| v1.29.1 | 訂正 | ズーム操作バー・全体表示・枠と背景・高さ制限を撤去し見た目をv1.28.0へ復帰。掴んで動かす移動、欄への寄せ、ホイール横移動のみ残す |
| v1.28.0 | 帳票様式 | KY案を実物の様式（危険予知活動ボード／危険予知活動記録）で表示。一覧・ボード・記録様式の切替とA4横印刷。4大重大災害の自動判定、危険度の大中小変換、重大性×可能性の機械割り当て |
| v1.27.5 | 訂正 | v1.27.4の拡張子列挙による3択シート回避が実機で無効と判明。accept を image/* に戻し長押しの控えも撤去。選ぶべき項目の明示とタップでの差し替えに方針変更 |
| v1.27.4 | 操作性 | 「写真を選ぶ」でiOSの3択シート（ファイルを選択／写真を撮る／フォトライブラリ）が挟まる問題に対応。accept を拡張子列挙に変更して撮影項目を外す。写真枠のタップもライブラリを開く方式へ。長押しで従来の image/* を開く控えを用意 |
| v1.27.3 | 修正 | 待機画面の工程別目安が内部処理の実測（転記22秒）のままで実機の1/6以下だったため、実機実測（転記5.6秒／千文字、接続4・復元2・目視12・整理7秒）に置換。転記の目安は送信文字数から自動計算する方式へ。進行度バーの時定数も実機に合わせて修正 |
| v1.27.2 | 最適化 | 長辺・品質を49通り実測し最適点を長辺520px・q22と確定。モードごとに動作点を直接指定し圧縮の試行を1回へ（従来は標準3回・速度優先10回で約5秒を無駄にしていた）。上限文字数を26／32／40千文字へ変更。後退は解像度優先へ。4:2:2・4:4:4と輪郭強調は効果なしと確認し不採用 |
| v1.27.1 | 修正 | 進捗判定の正規表現が「チャンク#k/N」の#に不一致で受信0件だった。未検出工程を後続へ合算し誤ったラベルを付けていた。圧縮時間がキャッシュのため0.0秒と出ていた。分割幅を6,000字へ戻し沈黙上限対策を強化 |
| v1.36.0 | 公開向け要約 | 事例をタグごとに最大100件まで機械抽出し、AIが最大3件を選定して理由を返す2段構成。候補ID照合、順位表示、記録出力を実装。 |

### 写真の選択口とiOSの3択シート（v1.27.5・実機検証済み）

iOSで `input type="file"` を押すと「ファイルを選択／写真を撮る／フォトライブラリ」の選択シートが出ます。**この項目をWebページ側から消す方法は存在しません。**

v1.27.4で `accept` に拡張子を列挙する回避策（`.jpg,.jpeg,.png,.heic,.heif,.webp`）を実装しましたが、**iPhone実機で3択シートが従来どおり表示され、効果がないことを確認**しました。iOSは `accept` の内容にかかわらずこのシートを出すため、この方向の回避は成立しません。拡張子指定はHEICが選べなくなる危険だけが残るため撤去し、`image/*` に戻しています。**今後この回避策を再実装しないこと。**

対処方針は「シートを消す」ではなく「**迷わせない・押す回数を減らす**」です。

| 用途 | input | accept | 挙動 |
|---|---|---|---|
| カメラで撮影 | photoCam | image/* + capture | カメラが直接起動（iOS 10.3以降。capture属性は有効） |
| 写真を選ぶ／写真枠タップ | photoInput | image/* | 3択シートを経てライブラリ |

* スマートフォンでは選ぶべき項目（フォトライブラリ）を画面上に明示し、省略できない仕様である旨を添える
* 一度選んだ後の差し替えは写真のタップのみで行える（枠内に「タップで写真を差し替え」を表示）

## 4. 過去の実測データ（設定変更時の参考）

以下は旧版台帳から引き継いだ記録で、今回再測定した値ではありません。現在の端末・モデル・外部AI APIでの性能や安全性を保証しません。原写真と実測ログは公開版に含めません。

### 処理時間の内訳（画面キャプチャ画像・33,052字）

転記22.1秒（61.0%）、目視8.0秒（22.1%）、所見のJSON生成5.4秒（14.9%）、デコード0.7秒（1.9%）、ブラウザ側の縮小0.06秒（0.2%）。合計36.3秒。

**この値はサンドボックス内部処理の実測であり、画面表示には使いません。** 同条件の実機（iPhone）では約6倍の時間がかかります（転記5.6秒／千文字＝内部処理の0.67ミリ秒／文字の約8倍）。利用者に見せる目安は必ず実機実測を根拠にします。

### 実機（iPhone・Edge）の工程別実測

| 工程 | 実測 | 備考 |
|---|---|---|
| 写真の読込・正規化 | 0.3秒 | 端末側。無視できる |
| 写真の圧縮 | 0.0〜5秒 | 試行回数に比例（v1.27.2で1回化） |
| 接続〜初回応答 | 4.3秒 | 通信環境とAI側の混雑に依存 |
| 転記＋目視 | 196.9秒 | 36千文字時。5.6秒／千文字 |
| 所見の整理 | 7.2秒 | |

合計208.5秒（標準36千文字時）。v1.27.2で標準を23千文字に変更したため、想定は約2分半に短縮。

### 圧縮比と判読性（6段階）

| 長辺／品質 | base64文字数 | 中核所見 | 全体所見 |
|---|---|---|---|
| 1200 / 85 | 265,484 | 100% | 100% |
| 900 / 72 | 128,388 | 100% | 96.7% |
| 760 / 65 | 88,420 | 100% | 90.0% |
| 640 / 60 | 62,388 | 100% | 83.3% |
| 500 / 45 | 34,108 | 85% | 60.0% |
| 380 / 35 | 17,992 | 45% | 30.0% |

500px以下でバケットの向き・鋼製架台・梯子の固定状態が判別不能になり、KY判定に使えません。

### 画像形式の比較（640px）

JPEG品質60が44,924字、WebP品質55が27,240字、AVIF品質50が22,644字。ただしiOSはWebP・AVIFの書き出しに非対応。

### 解像度・品質の最適点（v1.27.2の根拠／iPhone実写真）

長辺7水準×品質7水準の49通りを実測。転記時間は5.6秒／千文字で換算。

| 長辺(q22) | 文字数 | 転記予測 | SSIM | 追加1秒あたりの効率 |
|---|---|---|---|---|
| 340px | 14,280字 | 80秒 | 0.703 | — |
| 400px | 18,776字 | 105秒 | 0.749 | 1.83 |
| 460px | 23,608字 | 132秒 | 0.789 | 1.46 |
| 520px | 29,484字 | 165秒 | 0.821 | 0.99 |
| 600px | 37,404字 | 209秒 | 0.856 | 0.77 |
| 720px | 49,772字 | 279秒 | 0.894 | 0.49 |

限界効率が460→520pxで1.0を割るため、**最適帯は460〜520px**。

### 等コスト比較（同じ文字数なら解像度を優先すべき）

| 予算 | 解像度優先 | 画質優先 |
|---|---|---|
| 約23千字 | 520px q22 → 細部 **0.922** | 460px q32 → 細部 0.906 |
| 約18千字 | 460px q22 → SSIM **0.757** | 400px q32 → SSIM 0.749 |
| 約30千字 | 600px q22 → SSIM **0.825** | 520px q32 → SSIM 0.821 |

同一予算では常に解像度優先が勝ちます。後退ロジックで画質をq22に固定し解像度のみを落とす理由です。

### 色差サブサンプリングと輪郭強調（いずれも不採用）

4:2:2は文字数+4.3%、4:4:4は+12.3%に対しSSIMは0.7887→0.7886と無改善。**4:2:0が最適**。
輪郭強調は軽い強調でSSIM 0.7887→0.7944（+0.006）に対し文字数+16.7%、強い強調ではSSIM 0.7679へ悪化。圧縮効率を落とすだけで不採用。

### 送信文字数と所要時間の関係（実機2件から導出）

標準36千字→196.9秒、速度優先22千字→118.5秒。文字数38.9%減に対し時間39.8%減で、**時間は文字数の一次関数**（5.6秒／千文字、固定費−4.7秒）。往復回数比例の仮説では1往復49.2秒対39.5秒で25%食い違い、説明できない。**分割数ではなく総文字数が支配要因**。

### 画像の性質による差

画面キャプチャ（42万画素・平坦）は600px/q45で33,052字。iPhone実写真（1,200万画素・細かな模様が全面）は同設定で117,808字（3.6倍）。設定値の固定ではなく文字数の実測が必要な理由です。

### 符号化方式の比較（20,662バイトの画像）

16進数41,324字、Base32が33,064字、Base64が27,552字、Ascii85が25,828字、Base91が25,410字、Base64+zlibが25,848字。最良でも7.8%減に留まり、使用文字種が増えるため実際のトークン消費はむしろ悪化します。JPEG/WebPの再圧縮は無意味（むしろ増加）。

### 事例検索の過去の実測値

元DBの件数・分布・実例・到達率・送信量の統計は公開対象から除去しました。上位候補を広げてAIに選定させる設計は維持しています。公開版の架空データへ過去の実測結果を適用しないでください。

## 5. 過去の環境で採用しなかった方式

以下は当時のホスト環境での検証記録です。外部AI APIへ移行する際の普遍的な制約ではありません。特に画像直接送信の可否は移行先APIで再確認してください。

同じ提案を繰り返さないための記録です。

| 方式 | 結果 | 理由 |
|---|---|---|
| base64文字列をAIに目読させる | 不可 | zlib圧縮のため文字列から内容は特定できない |
| 画像を直接送るAPI（sendImage / sendMedia / sendMessage） | 存在しない | ブリッジは `callAgent` と `callTool` の2つのみ |
| データURIを document_reader に渡す | 空応答 | 読めない |
| ローカルパス・`file://` を渡す | 失敗 | `LINK_NEEDS_AUTH` |
| `POST /rest/api/v1/uploadchatfiles` | 不可 | 外部通信が遮断（TCPタイムアウト、HTTPSは403） |
| Graph APIでOneDrive/SharePointへ保存 | 不可 | 同上。資格情報の有無と無関係 |
| Glean MCP（AIアプリ連携） | 方向が逆 | Gleanが社内知識を外部AIへ提供する機能 |
| GleanをMCPホストとして書き込みツール接続 | 条件付き | 管理者設定必須、エージェント限定、ベータ |
| ポーリングによる自動再送・再開 | 撤去 | 進捗が画面に通知されず、起点が確保できず前進しない |
| KY事例集への蓄積（Markdown台帳） | 破棄 | 本質はKYの分析。1件70KB増で20件で破綻、同時保存も不可 |
| 符号化をBase85/Base91へ変更 | 不採用 | 削減7.8%、規則性喪失でトークンは減らない |
| JPEG/WebPの再圧縮 | 無意味 | 既に圧縮済みでサイズが増える |

### 既出事例の降格による重複回避（v1.36.0で不採用）

過去に提示した事例IDを端末に保存し、次回以降は類似度から減点する案を検討しました。母集団を順に使えるようになりますが、**「見たことがあるか」で機械的に外す方式は安全上妥当でない**と判断しました。同じ現場で繰り返し起きる危険は、繰り返し提示すべきものです。

代わりに候補を広げてAIに選ばせる方式を採りました。「今日の現場に効くか」で選ぶため、安全上の判断として適切です。端末をまたいで履歴が引き継がれない問題も生じません。

### 上位帯から日替わりで選ぶ方式（不採用）

上位10件から3件を日付で切り替える案です。実装は軽いものの、**同じ入力で結果が変わる**ため、KY活動の記録としての再現性が失われます。

## 6. 現時点で判明している制約

* 画面からのAI呼び出しはテキストのみ。画像を直接渡す引数がない
* 無通信が60秒続くと基盤側が打ち切る（実測で確定）
* ブリッジは工程の進捗やステップ更新を画面に通知しない
* サンドボックスは外部通信が遮断されている
* iOSはcanvasからWebP・AVIFを書き出せない
* HTMLとして保存し単体で開いた場合、解析機能は動作しない（AI呼び出しは会話の仕組みを経由）
* base64経路のトークン消費は直接送信の約20倍（1トークンが担う画素数は726対39）

## 7. 将来の実装案（未実装・再評価が必要）

公開版は匿名化・架空データ化までの準備版です。外部AIへの接続、サーバー構築、公開運用への適合性評価は実施していません。画像を直接送れるAPIと、認証情報をサーバー側で保管する構成を今後検討します。保存先や承認フローは利用先に合わせて決めます。

## 8. 作業手順（機能追加・修正時）

1. この台帳の第2章（現行機能）と第5章（採用しなかった方式）を確認する
2. 変更が既存機能を壊さないか、特に画面遷移とボタン描画の依存を確認する
3. 実装後は構文検証に加え、起動時の実行順序と画面遷移後のボタン描画を検証する
4. `APP_VERSION` と `APP_BUILD` を更新し、アプリ内の変更履歴に追記する
5. この台帳の第2章・第3章、必要なら第4章・第5章を更新する

版数の付け方は、機能追加でマイナー（v1.27.0）、不具合修正でパッチ（v1.26.3）とします。

## 9. 過去の不具合と再発防止

| 版 | 症状 | 原因 | 教訓 |
|---|---|---|---|
| v1.7.0 | 232秒・6回とも失敗、工程0のまま | 往復上限4に対し実作業は7往復。中断を許す指示がAIに着手しない口実を与えた | 上限は実測往復数から設定する。中断の許可は条件を絞る |
| v1.20.1 | チェックマークが見えない | 未定義のCSS変数を参照 | 変数の定義有無を確認する |
| v1.26.1 | 起動時にスクリプトが停止 | 初期化がモード定義より先に実行 | 構文検証だけでなく実行順序を検証する |
| v1.26.2 | STEP2で操作が詰まる | 画面遷移でボタン列が再描画されない | 遷移関数に描画を内包させる。動線の退行を確認する |
| v1.32.0 | 公開向け要約 | 事例データをgzip＋base64で内蔵。ブラウザで展開し、索引とタグ別プールを生成する。旧版の実事例・統計は公開版に含めない。 |
| v1.32.0 | 公開向け要約 | 事例データをgzip＋base64で内蔵。ブラウザで展開し、索引とタグ別プールを生成する。旧版の実事例・統計は公開版に含めない。 |
| v1.33.0 | 事例がKY案に反映されない | 指示が禁止中心で、使ってよい条件を示していなかった | プロンプトは禁止だけでなく、期待する動作を明示する |

## 10. 公開版の変更・確認記録

### 入力と決定

* 2026-09-14：添付のアプリHTMLと開発台帳について社外公開向けの確認・マスキングを依頼。
* 同日：利用者が「実事例を全て除去し、架空のデモ事例へ差し替える」方針を選択。
* 元ファイルを上書きせず、既存HTMLをベースに公開用コピーを作成。既存画面・検索アルゴリズムを作り直さない。

### 変更

* 内蔵実事例を全削除し、一般的な作業危険を題材にした新規の架空60件を内蔵。
* 会社名、実設備名、組織名、個人識別情報を含む共有URL、内部サイトID、実データ統計を本文・履歴・コメントから削除または一般化。
* 架空であることを画面・プロンプト・記録に明示。CSV取込時に機密情報の再混入を注意喚起。
* 帳票の構成は維持し、会社表記を公開デモ表記へ変更。用紙サイズはブラウザ側で指定。

### 確認状況

検証結果と未確認事項は、同梱の[公開前確認記録](PUBLIC_RELEASE_REVIEW.md)を参照してください。原ファイルのURL、原データ、マスキング前後の対照表は漏えい防止のため同梱しません。

* 検証で、事例の件名・本文に付けた「架空」というラベルが既存の「鉄塔・電柱」タグの正規表現（架空線の語）に一致することを確認。検索式は変更せず、索引対象の件名を「【デモ】」、本文の補足を「模擬条件」に修正。画面・プロンプト・出力の架空注記は維持。
* 最終版でgzip展開60件、3軸30タグの文脈付き候補検索、候補IDの存在照合、架空注記、CSV読込、JavaScript構文を確認。実ブラウザ・実AI接続は未検証。
* 「生物」タグのみの短い入力は、類似度のしきい値により該当なし。蜂等の具体的な状況を伴う検索では候補が返ることを確認。検索式を変えて無理に候補を出す処理は追加しない。

### 未完了・次の作業

* 外部AI APIへの接続リファクタリング。利用者のAPIキーはHTML・Git・ブラウザストレージへ埋め込まず、サーバー側の秘密情報として扱う構成を推奨。
* 公開前にソースコード・帳票様式・旧版技術記録の社外公開許諾を確認。今回の削除処理は公開承認ではありません。
* 実ブラウザでの起動・5画面遷移・写真入力・AI応答・JSON/CSV出力・印刷を通して確認。
* Gitの履歴、著者名・メール、Issue、画像、添付、ログ、環境設定は今回の添付2ファイルの点検範囲外。

※重要な判断には必ず原文を確認してください。
